[package]
name = "optix-sys"
version = "0.1.0"
edition = "2021"
description = "Raw FFI bindings to NVIDIA OptiX 9.0.0 ray tracing SDK"
license = "MIT"
links = "optix"
build = "build.rs"

[dependencies]
libloading = "0.8"

[build-dependencies]
bindgen = "0.72"
